#ifndef SNESRECOMP_PLATFORM_GLSL_STB_IMAGE_COMPAT_H
#define SNESRECOMP_PLATFORM_GLSL_STB_IMAGE_COMPAT_H

/* Match the include path used by snesrecomp's shared glsl_shader.c. */
#include <stb_image.h>

#endif
